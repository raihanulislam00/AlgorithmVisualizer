//  @type {import('next').NextConfig}
const nextConfig = {
    images: {
        unoptimized: true
    },
    // output: 'export', // Outputs a Single-Page Application (SPA).
    // distDir: './build', // Changes the build output directory to `./dist`.
    // assetPrefix: '/AlgorithmVisualizer', // Comment out for local development
    // basePath: '/AlgorithmVisualizer', // Comment out for local development
}
   
export default nextConfig;